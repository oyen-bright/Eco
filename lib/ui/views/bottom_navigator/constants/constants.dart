part of app_bottom_navigator;

@immutable
class Constants {
  const Constants._();

  static const double bottomNavBarHeight = AppSizes.size65;
  static const Size iconSize = Size(AppSizes.size24, AppSizes.size24);
}
